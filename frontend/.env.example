# Clerk publishable key — get from https://dashboard.clerk.com
# Settings > API Keys (starts with pk_test_ or pk_live_)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Optional: override the backend URL (defaults to /api proxy in dev, or nginx proxy in prod)
# VITE_API_BASE_URL=http://localhost:8000
